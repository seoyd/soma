campaign_id = "campaign_research"
description = "Research evidence campaign over multiple local batch matrices"
matrix_config_paths = [
  "examples/soma_batch_baseline_only.toml",
  "examples/soma_batch_research.toml",
]
output_root = "target/soma_campaigns"
evidence_store_path = "target/soma_evidence"
run_id = "campaign-research-seed"
continue_on_failure = true
require_all_matrices_pass = false
min_usable_datasets = 2
min_total_outcome_records = 20
min_regime_coverage_count = 2
min_passed_runs = 2
min_data_quality_score = 0.8
max_allowed_drawdown_regression_pct = 0.02
max_allowed_calibration_regression = 0.02
max_allowed_risk_governor_instability = 0.15
allow_persona_expansion_recommendation = false
created_at_ms = 42
allow_evidence_overwrite = true
reason_codes = ["DeterministicPath"]
