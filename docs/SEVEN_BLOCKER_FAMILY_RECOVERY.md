# Seven Blocker Family Recovery

The Sprint 88 recovery bundle keeps three truths separate:

1. family-level reduction and isolation reports
2. real no-run gate observation
3. real full workspace gate observation

The seven-family report keeps the queue, primary next family, and remaining blockers explicit. `committee_cli_safety` stays isolated unless the same CLI-safety guarantees can be preserved in a safer split.
