# Sprint 48 restart report

## 1. Sprint summary

Implemented an offline-first prospective Shadow challenge contract and sealed
its local capsule before any future-row inspection or provider request.

## 2. Honest AI maturity status

The system remains a deterministic Shadow research prototype; prospective
predictive value, autonomous learning, and trading readiness are unproven.

## 3. Complete baseline verification

Before changes, sequential default and Metal workspace suites passed with the
four existing dead-code warnings only.

## 4. Historical immutable-input verification

The create path re-verifies the selected local snapshot digest, chronology,
evidence ledger, frozen regime packs, and pack digests offline.

## 5. Sprint 47 result reproduction

The frozen replay still resolves the single older-regime validation-and-test
InSupport research path; the cross-regime result remains sparse evidence only.

## 6. Existing prospective cutoff

The cutoff is derived from the immutable consumed-evidence ledger and remains
exclusive. No timestamp at or before it can enter the prospective vault.

## 7. Qualified candidate resolution

Resolution requires exactly one selected forensic candidate with both
validation and test InSupport. Missing or multiple matches fail closed.

## 8. Candidate source campaign/regime/window

The local capsule records the resolved campaign, older chronological regime,
window, candidate identity, and checkpoint relationship without a fixed index.

## 9. Candidate checkpoint result

The selected checkpoint is frozen by its exact epoch and head digest. It has no
permission beyond Shadow research.

## 10. Candidate artifact digests

The local candidate contains encoder, representation-normalizer, head, feature,
sequence, label, support, collapse, backend, and aggregate artifact digests.

## 11. Linear comparator resolution

Linear is frozen from the same selected window's causal historical training and
validation boundary and cannot be refit on prospective rows.

## 12. Linear comparator digest

The local comparator preserves parameter bits and a deterministic artifact
digest; ordinary status output exposes neither.

## 13. Constant comparator resolution

Constant is frozen from training prevalence only, never future, validation, or
prospective label prevalence.

## 14. Constant comparator digest

The local Constant artifact records its training-boundary digest, exact
probability bits, parameter digest, and artifact digest.

## 15. Challenge-capsule implementation

The capsule freezes candidate, two comparators, cutoff, feature/label/support
policies, prediction horizon, evidence/prediction/evaluation/opening policies.

## 16. Challenge-policy validation

Capsule validation rejects non-Shadow candidates, missing/different comparator
types, corrupted artifacts, non-single-request policy, and digest mismatch.

## 17. Pre-registered primary metric

Primary metric is Brier score on support-qualified prospective prediction events.

## 18. Pre-registered secondary metrics

Reliability, resolution, uncertainty, guarded AUC, high-confidence errors,
coverage, abstention, support failure, diversity, and collapse are diagnostic.

## 19. Evaluation eligibility requirements

The capsule pre-registers minimum finalized rows, mature events,
support-qualified events, positive labels, AUC negative-label guard, and zero
technical-failure tolerance.

## 20. Winner/verdict rules

A positive result requires both frozen-baseline Brier improvements, integrity,
sample eligibility, and no severe collapse; no result grants live authority.

## 21. One-time opening policy

Opening requires later explicit authorization, exact capsule/vault/journal
digests, eligible counts, and rejects a second opening.

## 22. Challenge invalidation policy

Candidate/comparator/policy drift, early future or label access, vault/journal
modification, duplicate opening, and integrity failures permanently invalidate.

## 23. Capsule semantic digest

The sealed capsule digest is `8884354dbb27a619`; it excludes paths, timestamps
from wall clocks, terminal formatting, and credentials.

## 24. Challenge sealing result

The capsule is locally sealed and re-read with matching digest. Its initial
registry state is Sealed, with an empty vault and journal.

## 25. Pre-registration commit/push result

Pending the Phase A source-and-policy commit at the time this report is written.

## 26. Post-commit capsule verification

Deferred until the Phase A commit has been pushed; it must compare the local
sealed capsule digest with this report's registered digest.

## 27. Future-row availability plan

No future row is inspected before Phase A completion. Availability is later a
blind count/dry-run question, never a label or OHLCV display.

## 28. Ethical request dry run

The initial dry run has request count 0. Any later request is bounded to one,
one concurrency slot, zero retries, and finalized daily rows only.

## 29. Network-consent result

No dedicated prospective local consent has been accepted by this offline phase.

## 30. Actual provider request count

0.

## 31. Future finalized-row acquisition result

No future row has been acquired or inspected.

## 32. Prospective vault result

The local append-only vault is empty, unopened, label-free, and separate from
development evidence.

## 33. Prospective vault digest

Initial vault digest is `537fda13a82acb82`.

## 34. Historical-development separation audit

Vault admission rejects cutoff-or-earlier, duplicate, non-finalized, unordered,
and malformed row references; future evidence cannot become development data.

## 35. Sealed prediction-event count

0.

## 36. Support-qualified prediction count

0.

## 37. Prospective abstention count

0.

## 38. Awaiting-label-maturity count

0.

## 39. Mature-but-sealed label count

0.

## 40. Prediction-journal digest

Initial journal digest is `2a1d836d75505b7d`.

## 41. Challenge-registry digest

Initial registry digest is `1eb969ed6c33c514`.

## 42. Blind status-report result

Text and JSON status contain challenge state, counts, and digests only; no
future OHLCV, prediction, comparator probability, label, return, or path.

## 43. Interim-evaluation prohibition audit

No Brier, accuracy, AUC, calibration, per-event error, or comparator result is
computed before a valid one-time opening.

## 44. One-time evaluation readiness

Not ready: no finalized prospective row, event, or mature label exists.

## 45. Current challenge status

Sealed and awaiting the pre-registration commit boundary.

## 46. Network firewall

Candidate resolution, capsule sealing, local reread, and status use zero
provider calls and zero transport constructions.

## 47. Evidence-ledger result

The immutable historical ledger remains valid with digest `a3249852cc155c5e`.

## 48. Prospective holdout result

The existing holdout remains `PolicySealedNoFutureRows`, unopened, and without
label access.

## 49. Layered eligibility

The candidate, comparators, predictions, and abstentions are all ShadowOnly;
promotion, voting, and execution remain false.

## 50. Backend result

The implementation retains the existing CPU policy and passes the Metal-feature
baseline without adding GPU training.

## 51. Official Mamba conformance status

`OfficialOracleExecutionBlocked` remains unchanged; no official parity claim is
made.

## 52. Storage/evidence immutability

The ignored local state atomically writes then reopens and validates capsule,
vault, journal, and registry digests. Historical snapshots are not rewritten.

## 53. Hardcoding audit

Production resolution derives candidate/comparator identity, cutoff, ranges,
and outcomes from immutable inputs. No real artifact ID, timestamp, prediction,
or verdict is hardcoded.

## 54. Model/trading isolation

No feature, label, threshold, optimizer, candidate set, encoder, committee,
Chair, Risk Governor, PaperBroker, account, order, or runtime LLM path changed.

## 55. Files added

Added the prospective challenge policy and this restart report.

## 56. Files changed

Changed existing historical-evidence, learning-campaign, model-export, CLI, and
prospective-holdout policy files.

## 57. Tests added or reused

Added capsule mutation/determinism, state/vault fail-closed, sealed journal, and
atomic local round-trip tests; reused default and Metal baselines.

## 58. Complete final verification

Pending after the Phase A report and commit boundary; it will run default and
Metal sequentially.

## 59. Build-cache result

No cache deletion is required for the prospective implementation.

## 60. Instruction-file boundary result

The temporary instruction input was used only for task scope and was not added
to code, documentation references, capsules, journals, vaults, or build input.

## 61. Unrelated-file boundary result

The pre-existing unrelated untracked file remains unread, unmodified, unstaged,
and uncommitted.

## 62. Risk/security review

The implementation fails closed on ambiguity, corruption, invalid transitions,
early rows, non-finalized rows, duplicate events, premature labels, and opening.

## 63. What was proven

One historical research path can be uniquely frozen into a prospective
comparison contract without accessing future evidence or outcomes.

## 64. What remains unproven

No prospective prediction, calibration, benchmark comparison, profitability,
live readiness, or repeated predictive value has been proven.

## 65. Deferred items

Future-row acquisition, prediction-event generation, label maturity, and
one-time evaluation require the completed pre-registration boundary and later
explicit authorization.

## 66. Commit/push result

Pending the Phase A commit at the time this report is written.

## 67. Next gstack Sprint recommendation

After Phase A verification, confirm the local registry, perform only a blind
zero-or-one-request availability action under explicit consent, and keep all
labels sealed.
