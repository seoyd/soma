# Workspace Acceptance Attempt V3

`WorkspaceWideAcceptanceAttemptV3` records the Sprint 85 validation state:

- `cargo fmt` and `cargo check`
- grouped domain suites
- safety guard, CLI safety, and determinism suites
- representative and safety smoke
- full workspace started / finished / passed truth

Focused suites speed up iteration, but `FullWorkspaceStillBlocked` remains the honest result until the full workspace actually finishes.

