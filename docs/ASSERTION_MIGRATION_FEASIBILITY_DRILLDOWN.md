# Assertion Migration Feasibility Drilldown

Sprint 114 inventories assertion counts, kinds, dependencies, and migration complexity by suspect target.
The drilldown blocks readiness unless migration feasibility is proof-backed.
