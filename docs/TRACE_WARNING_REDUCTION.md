# Trace Warning Reduction

`TraceCompletenessWarningReductionReport` reduces warnings only when existing evidence or an explicit conservative reason supports the reduction.

Warnings are not silently removed. Remaining warnings stay visible in the report and in the Control Tower diff triage panel.
