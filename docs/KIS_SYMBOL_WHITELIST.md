# KIS symbol whitelist

- Whitelists stay compact, deterministic, and local-only.
- Domestic symbols use provider symbols such as `005930`.
- Overseas symbols require an exchange code such as `NASD`.
- Wildcards and all-symbol scans are denied.
- Example: `examples/soma_kis_symbol_whitelist_compact.toml`.
