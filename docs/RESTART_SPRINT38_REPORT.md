# Restart Sprint 38 report

## Scope

Implemented validation checkpoint trajectories and a validation-only signal-existence gate for the fixed C0–C3 forensic candidates. The implementation records every epoch, preserves rejected reasons and eligible frontiers, uses train-prevalence Constant comparison, and keeps the test sealed when no candidate has an eligible checkpoint.

## Safety result

Shadow abstentions carry false voting, execution, and promotion permissions. Selected checkpoints alone may receive one test evaluation; a collapsed test after healthy validation is reported as temporal generalization collapse. No provider, snapshot, evidence identity, committee member, trading, GPU training, or Mamba-gradient path was changed.

## Verification

Complete default and Metal baseline suites were run sequentially before edits. Final-tree verification and the offline campaign replay are recorded with the delivery result after the implementation checks complete.

## Limitations

This remains a single-series, offline, experimental frozen-encoder assessment. Brier without resolution and discrimination evidence is not treated as a useful-signal conclusion. No profitability, promotion, voting, execution, live readiness, or official conformance claim is made.
