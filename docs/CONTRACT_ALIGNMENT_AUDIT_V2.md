# Contract Alignment Audit V2

`ContractAlignmentAuditV2` explains whether an unexpected diff is driven by dataset/schema/label contract change or whether contracts remain aligned and the diff should be explained elsewhere.

The audit reuses Sprint 69 snapshot refs and summary-card contract fields instead of introducing a new snapshot format.

It remains:

- research-only
- paper-only
- local-only
- read-only
- deterministic
