# Mamba3Fin-lite Contract

Sprint 63 adds a **contract**, not a runtime.

- external prototype only
- prediction CSV contract required
- model card contract required
- offline evaluation contract required
- no runtime integration
- no training
- no live inference

`ContractReady` only means the offline artifact contract is complete. It does **not** mean Mamba runtime exists.

