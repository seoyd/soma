# Acceptance Truth Gate V17

Focused tests, CLI smoke, cargo build, no-run, cargo JSON, artifact ordering, and timeout cleanup remain supporting-only. Only a finished and passed `cargo test --workspace --quiet` can claim full acceptance.
