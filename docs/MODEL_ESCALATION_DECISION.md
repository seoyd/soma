# Model escalation decision

Sprint 55 adds `model-escalation-decision` to pick the next safe step.

Possible directions:

- keep baseline and evidence work active
- improve baseline signal first
- build sequence dataset first
- allow an external Mamba3Fin-lite prototype
- keep Mamba deferred

Rust-native Mamba runtime is always rejected in this sprint.
