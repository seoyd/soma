# Real No-Run / Full Observation V17

`cargo test --workspace --no-run --quiet` and `cargo test --workspace --quiet` remain separate observations. No-run is not full acceptance; a full workspace pass is the only acceptance path.
