# Mamba3 Application Timing

Sprint 60 keeps **Mamba3 runtime deferred**.

The timing report exists to answer *when* Mamba-related work could be considered, not to add the runtime now. The answer remains conservative:

1. evidence quality must improve first
2. sequence dataset readiness must pass first
3. only an external prototype bridge could be considered later
4. Rust runtime implementation is still out of scope

This means:

1. no Mamba runtime command is added
2. no Rust-native inference is added
3. no Rust-native training is added
4. no claim is made that the model path is ready now
