# Prefer Metal on Apple, otherwise CPU; CUDA opt-in by feature
[target.'cfg(target_os = "macos")']
rustflags = ["-C", "target-cpu=native"]

[target.'cfg(target_os = "linux")']
rustflags = ["-C", "target-cpu=native"]
